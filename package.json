{
  "name": "notely2",
  "version": "0.0.1",
  "description": "",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "awesome-typescript-loader": "^0.3.0-rc.2",
    "http-server": "^0.8.0",
    "traceur-loader": "^0.6.3",
    "tsd": "^0.6.4",
    "typescript": "^1.5.3"
  },
  "scripts": {
    "start": "./node_modules/.bin/tsc --watch",
    "build": "./node_modules/.bin/tsc",
    "prestart": "npm install && ./node_modules/http-server/bin/http-server -p 8000 -a localhost"
  }
}
